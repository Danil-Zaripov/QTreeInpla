# Since text editors do not include IDE features for Inpla
# file formatting may end up overlooked, hence pre-commit
repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v3.2.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
